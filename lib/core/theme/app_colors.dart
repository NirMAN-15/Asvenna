import 'package:flutter/material.dart';

class AppColors {
  // Brand Agricultural Greens
  static const Color primary = Color(0xFF2E7D32); // Emerald Forest Green
  static const Color primaryDark = Color(0xFF1B5E20);
  static const Color primaryLight = Color(0xFF4CAF50);
  static const Color primarySoft = Color(0xFFE8F5E9);
  
  // Secondary Earthy & Gold accents
  static const Color secondary = Color(0xFF8D6E63);
  static const Color accent = Color(0xFF00897B); // Teal green
  static const Color goldAccent = Color(0xFFFFB300);

  // Risk Engine Alert Levels
  static const Color riskSafe = Color(0xFF2E7D32);       // Green - Balanced / Safe to plant
  static const Color riskSafeBg = Color(0xFFE8F5E9);
  static const Color riskModerate = Color(0xFFF57C00);   // Amber - Approaching saturation
  static const Color riskModerateBg = Color(0xFFFFF3E0);
  static const Color riskCritical = Color(0xFFD32F2F);   // Red - Saturated / Over-planted
  static const Color riskCriticalBg = Color(0xFFFFEBEE);

  // Background & Surfaces
  static const Color background = Color(0xFFF6F8F6);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color cardBg = Color(0xFFFFFFFF);
  static const Color divider = Color(0xFFE0E6ED);

  // Typography Colors
  static const Color textPrimary = Color(0xFF1A2E1A);
  static const Color textSecondary = Color(0xFF556955);
  static const Color textMuted = Color(0xFF8C9E8C);
  static const Color textLight = Color(0xFFFFFFFF);

  // Marketplace & Status
  static const Color badgeFresh = Color(0xFF00897B);
  static const Color badgeHarvest = Color(0xFFE65100);
}
